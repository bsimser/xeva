 # need to convert test projects to the spec style.
 # need to convert specs to use the AMC.
 # need a dispatch/map/route solution
 # "view helper" or "helper method solution" at controller level? fillout the link idea
 # correct fluent interface issues with controller
 # need  some kind of controller catalog / repository
 # sample application
 # msbuild script, harvest outputs to /build/package, create and teardown /build/temp
 # add obvious validators, address naming issues in XEVA.Framework.Shared
 # nhibernate mappings as fluent interface in XEVA.Framework.Store/Model?
 # port over the remoting channel / service activator thing
 # bring the dto base class in, external dsl for dtos?